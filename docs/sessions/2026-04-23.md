# Session: 2026-04-23

## Goal

_(awaiting user — what are we working on today?)_

## Progress

- **Project scaffolded** (carried over from 2026-04-22 evening): folder renamed `ResizerAutomatic` → `AspectRatioUnifier`, Directions docs copied, numbered folder structure created (`01_Project`, `02_Design/Exports`, `03_Screenshots`, `04_Exports`), `.gitignore` seeded, initial git commit made
- **`CLAUDE.md`** written — project brief, tech stack, CropBatch reuse pointers, entry points
- **`PROJECT_STATE.md`** written — phase `discovery`, focus on Xcode scaffold via CropBatch clone
- **`decisions.md`** seeded with three durable entries:
  1. Positioning — specialist app, not a CropBatch mode, not CSS-only
  2. Target-proposal algorithm — B (histogram picker) primary + A (auto mode) fast path
  3. Code reuse — clone-rename CropBatch's Xcode project (cookbook §34)
- Stale session logs and `TASKS.md` from the Directions template were pruned

## Decisions

All three durable decisions are in `decisions.md`. Nothing new today beyond what was captured yesterday.

## Open questions carried forward

- Bucketing tolerance for aspect-ratio grouping: ±1% or ±0.5%?
- Tie-break rule when two buckets tie on count — tentatively: closer to overall median size wins
- Upscale policy when chosen target exceeds some source dims — warn / refuse / allow?

## Next

- `docs/cookbook/34-xcodeproj-clone-rename.md` — read before first `cp -R` of the CropBatch project
- Clone CropBatch's `.xcodeproj` into `01_Project/AspectRatioUnifier`, rename target + scheme + bundle id
- Strip CropBatch-specific views: crop editor, grid split, blur, watermark, snap-to-edge
- Keep: import pipeline, preview, resize+center-crop math, export pipeline, App Shell, Theme
- First new code: `AspectRatioBucket` model + analysis pass over imported `[URL]`
