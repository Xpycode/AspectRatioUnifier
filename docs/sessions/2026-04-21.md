# Session: 2026-04-21

## Goal
Add a cookbook entry for design tokens (typography, spacing, iconography, radii) covering both macOS apps and web projects, then verify and correct the Penumbra-as-reference-implementation mental model against actual source.

## Context
- Previous session: [2026-04-19](2026-04-19.md) — project identity persistence
- Current phase: maintenance / knowledge capture
- Trigger: user asked whether there's a section on graphical element usage (fonts and so on); scope expanded to include research on web + app best practices

## Progress

### Completed
- [x] Created `docs/cookbook/39-design-tokens.md` — typography scale (SF Pro, base 13, ratio 1.25), 8pt spacing grid with `buttonVertical = 6` off-grid tolerance, SF Symbol weight/scale conventions, corner radii (cap 12pt, anti-Tahoe), CSS `clamp()` fluid translation for web
- [x] Added row 39 to `PATTERNS-COOKBOOK.md` index
- [x] Added trigger keywords for 39 to `~/.claude/CLAUDE.md`
- [x] Updated `docs/cookbook/00-app-shell.md`:
  - Source line now cites exact path `1-macOS/Penumbra/01_Project/Penumbra/` + 5 canonical files
  - Added "Ignore archived copies" block listing 6 stale snapshot locations
  - Added "Architecture note — it's SwiftUI, not AppKit" correcting the mental model
  - New §2.1 "Theme is a floor, not a ceiling" documenting additive extension policy + sanctioned-extensions table
- [x] Restructured memory system — MEMORY.md was bloated (violating "index not content" rule); split into dedicated files:
  - `memory/app_shell_canonical.md` (project)
  - `memory/feedback_macos_aesthetic.md` (feedback)
  - `memory/xcode_previews_tool.md` (reference)
  - `memory/MEMORY.md` now a clean 4-line index, all entries ≤150 chars

### Discovered

**6 stale Penumbra snapshots exist.** The original CLAUDE.md / MEMORY.md pointer said `1-macOS/Penumbra/` which matched multiple paths. Concrete archived copies that will drift:
- `/Users/sim/XcodeProjects/XcodeArchive/VIDEO Apps/Penumbra/`
- `/Users/sim/XcodeProjects/XcodeArchive/VIDEO Apps/VideoMetadataEditor/AppsForReference/Penumbra/`
- `/Users/sim/XcodeProjects/1-macOS/VIME/01_Project/Archive/Penumbra/`
- `/Users/sim/XcodeProjects/1-macOS/medialibrary/01_Project/Reference/Penumbra/`
- `/Users/sim/XcodeProjects/1-macOS/VideoMetadataEditor/02_Design/Reference/Penumbra/`
- `/Users/sim/XcodeProjects/1-macOS/TimeCodeEditor/old-projects/Penumbra/`
Live path is `/Users/sim/XcodeProjects/1-macOS/Penumbra/01_Project/Penumbra/`.

**Penumbra is SwiftUI, not AppKit.** User's mental model treated Penumbra as "UIKit [AppKit] instead of SwiftUI." Actual architecture: SwiftUI-first with `NSViewRepresentable` drop-ins in exactly 5 files — `KeyInputView` (NSEvent monitor), `ShortcutRecorder`, `PlayerViewController` (AVPlayerView), `Views/MouseTrackingView` (NSTrackingArea), `Utils/View+SplitViewAutosave` (NSSplitView autosave). Everything else is pure SwiftUI. What makes Penumbra canonical is the *discipline* of the SwiftUI shell (FCP-style flat toolbar, hiddenTitleBar, dark Theme), not the AppKit interop.

**Sigil divergence is legitimate extension, not drift.** Sigil's `Theme.elevatedBackground (0.20)` / `separator` / `tertiaryText` have 12 concrete uses across 4 files (`VolumeDetailView`, `SidebarView`, `IconDropZone`, `VolumeRow`). The volume-browser domain legitimately needs a 3rd surface tier for cards + drop-target elevation. Resolved by promoting into §2.1 policy: floor tokens (`primaryBackground 0.10`, `secondaryBackground 0.15`) are inviolable; apps may extend *additively* when domain requires it.

**Concrete Penumbra metrics extracted** (now in `39-design-tokens.md` with file:line citations):
- Window min: 1400 × 800 (`PenumbraApp.swift:19`)
- InfoStrip height: **25** (deliberately off-grid — ribbon feel, not toolbar row)
- ControlsRow / Timeline: 50 each
- Bottom queue panel: 250
- ActionBar: 40
- Main pane min: 650
- Inspector: 220 / 300 / 500
- Queue settings: 220 / 250 / 400
- FCPToolbarButtonStyle: h:8 / v:6 / radius:4 / border:1
- Toolbar icon frame: 16×16

### Decisions Made
- **Option 2** for design tokens: new dedicated file `39-design-tokens.md`, not expansion of `00-app-shell.md` (keeps shell focused on structure, lets token file own visual tokens)
- **Typography scale:** base 13, ratio 1.25, SF Pro; allowed weights `.thin / .light / .regular / .medium / .semibold` only (no `.bold / .heavy / .black` — breaks FCP aesthetic); `.thin` size-gated to ≥24pt (WCAG contrast floor below that)
- **Spacing scale:** 8pt grid with documented 4pt half-step and 6pt off-grid exception (FCPToolbarButtonStyle buttonVertical)
- **Corner-radius cap:** 12pt hard max; `Radius.sm = 4` grounded in `FCPToolbarButtonStyle.cornerRadius(4)`
- **Theme policy:** floor (mandatory 5 tokens) + sanctioned extensions (Sigil's 3 extras documented in table); apps may never override floor values
- **Memory hygiene:** MEMORY.md is strictly an index (≤150 char one-liners, no inline content); dedicated file per memory

### Blockers
- None

### Pending
- **Git/GitHub workflow discussion** — user questioning the "Never commit directly to main" rule in their own CLAUDE.md. Rule exists for collaboration gates but user is solo dev. Proposed loosening to trunk-based solo workflow with branches as optional for experiments / large features. Awaiting user decision on replacement wording and auto-push policy.

## Next Session
- Resolve git workflow CLAUDE.md update (user's choice on exact wording)
- Migrate Penumbra's live `Theme` struct to use the new nested enums (`Theme.Font`, `Theme.Space`, `Theme.Radius`) so it matches `39-design-tokens.md` — this makes Penumbra the executable reference, not just the documented one
- Once Penumbra is migrated, run `/shell-check` across the other apps to surface magic-number `.padding(` / `.font(.system(size:)` / `.cornerRadius(` literals that should become tokens
- Consider: do Sigil's `separator` and `tertiaryText` deserve promotion into the floor, or are they still app-specific? Table in §2.1 lets future sessions decide as more apps need them.

## Notes
- The cookbook gained entries #40 (spaces-plist-backend), #41 (web-hero-floating-icons), #42 (web-native-dialog-lightbox) via linter/user edits mid-session — noted, no action needed
- Key correction for future sessions: "Penumbra is the reference" does not mean "Penumbra is AppKit." It means "Penumbra is the SwiftUI reference with disciplined FCP-flavoured chrome."
